module.exports = {
  access_token:    process.env['LINE_CHN_ACCESS_TOKEN'],
  channel_id:      process.env['LINE_CHN_ID'],
  channel_secret:  process.env['LINE_CHN_SECRET'],
  reply_api_url:   process.env['REPLY_API_URL'],
}
